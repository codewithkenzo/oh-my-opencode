export const TOOL_NAME = "wsl_notify"

export const TOOL_DESCRIPTION = `Send Windows toast notification from WSL. Use when session needs user attention.

Only works in WSL environment. Falls back to console log on other platforms.`
