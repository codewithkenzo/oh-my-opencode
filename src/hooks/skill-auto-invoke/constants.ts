export const SKILL_MANDATE = `## Skill Awareness (Auto-Injected)

Before ANY action, check if a relevant skill exists:
1. Use \`find_skills\` to discover domain-specific skills
2. If even 1% chance a skill applies, invoke it via the \`skill\` tool
3. kenzo-* skills take priority - they encode battle-tested project patterns
4. Skills are the #1 asset - every delegation MUST load ALL relevant skills`
