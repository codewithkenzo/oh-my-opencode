export const SUGGESTION_MESSAGE = (todoCount: number) =>
  `💡 ${todoCount} todos created. Consider using \`ticket_create\` instead — tickets support dependencies, priorities, and better tracking than todos.`

export const MIN_TODOS_FOR_SUGGESTION = 2
